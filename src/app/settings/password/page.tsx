import { PasswordForm } from "@/app/settings/password/PasswordForm";
import React from "react";

type Props = {};

export default function page({}: Props) {
  return (
    <div>
      <PasswordForm />
    </div>
  );
}
