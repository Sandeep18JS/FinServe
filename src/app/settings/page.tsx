import { EditForm } from "@/app/settings/EditForm";
import React from "react";

type Props = {};

export default function Settings({}: Props) {
  return (
    <div>
      <EditForm />
    </div>
  );
}
